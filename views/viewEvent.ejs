<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= event.name %> - Event Details</title>
    <style>
        :root {
            --gold: #FFD700;
            --dark-gold: #D4AF37;
            --black: #000000;
            --white: #FFFFFF;
            --light-gray: #F5F5F5;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--black);
            color: var(--white);
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            border-bottom: 2px solid var(--gold);
            margin-bottom: 30px;
        }
        
        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--gold);
        }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .user-info span {
            color: var(--gold);
        }
        
        .logout-btn {
            background-color: var(--gold);
            color: var(--black);
            border: none;
            padding: 8px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        
        .logout-btn:hover {
            background-color: var(--dark-gold);
        }
        
        .event-container {
            background-color: var(--white);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.2);
            color: var(--black);
            margin-bottom: 30px;
        }
        
        .event-header {
            background-color: var(--gold);
            padding: 20px;
            color: var(--black);
        }
        
        .event-title {
            font-size: 2rem;
            margin: 0;
        }
        
        .event-category {
            display: inline-block;
            background-color: var(--black);
            color: var(--gold);
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 0.9rem;
            margin-top: 10px;
        }
        
        .event-body {
            padding: 25px;
        }
        
        .event-meta {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .meta-item {
            display: flex;
            align-items: center;
            gap: 8px;
            color: #666;
        }
        
        .meta-item i {
            color: var(--gold);
        }
        
        .event-description {
            margin: 25px 0;
            line-height: 1.6;
            color: #444;
        }
        
        .rating-section {
            margin: 30px 0;
            padding: 20px;
            background-color: rgba(255, 215, 0, 0.1);
            border-radius: 8px;
            border-left: 4px solid var(--gold);
        }
        
        .rating-title {
            color: var(--gold);
            margin-bottom: 15px;
        }
        
        .rating-stars {
            display: flex;
            gap: 5px;
            margin-bottom: 15px;
        }
        
        .star {
            font-size: 1.5rem;
            color: #ccc;
            cursor: pointer;
            transition: color 0.2s;
        }
        
        .star:hover, .star.active {
            color: var(--gold);
        }
        
        .comment-form {
            margin-top: 20px;
        }
        
        .form-group {
            margin-bottom: 15px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: var(--gold);
        }
        
        .form-group textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: var(--light-gray);
            min-height: 100px;
            resize: vertical;
        }
        
        .btn {
            background-color: var(--gold);
            color: var(--black);
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        
        .btn:hover {
            background-color: var(--dark-gold);
        }
        
        .comments-section {
            margin-top: 40px;
        }
        
        .comment {
            padding: 15px;
            border-bottom: 1px solid #eee;
            margin-bottom: 15px;
        }
        
        .comment-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        
        .comment-user {
            font-weight: bold;
            color: var(--black);
        }
        
        .comment-rating {
            color: var(--gold);
        }
        
        .comment-text {
            color: #555;
            line-height: 1.5;
        }
        
        .no-comments {
            color: #777;
            text-align: center;
            padding: 20px;
        }
        
        footer {
            text-align: center;
            margin-top: 50px;
            padding: 20px 0;
            border-top: 2px solid var(--gold);
            color: var(--gold);
        }
    </style>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">EVENT DETAILS</div>
            <div class="user-info">
                <span><%= user.username %></span>
                <a href="/logout" class="logout-btn">Logout</a>
            </div>
        </header>
        
        <div class="event-container">
            <div class="event-header">
                <h1 class="event-title"><%= event.name %></h1>
                <span class="event-category"><%= event.category %></span>
            </div>
            
            <div class="event-body">
                <div class="event-meta">
                    <div class="meta-item">
                        <i class="far fa-calendar-alt"></i>
                        <span><%= event.date %> at <%= event.time %></span>
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-map-marker-alt"></i>
                        <span><%= event.location %></span>
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-user"></i>
                        <span>Hosted by: <%= event.creator_name %></span>
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-envelope"></i>
                        <span><%= event.contact_email %></span>
                    </div>
                    <div class="meta-item">
                        <i class="fas fa-phone"></i>
                        <span><%= event.contact_info %></span>
                    </div>
                </div>
                
                <div class="event-description">
                    <%= event.description %>
                </div>
                
                <% if (!user.isAdmin && !user.isSuperAdmin) { %>
                    <div class="rating-section">
                        <h3 class="rating-title">Rate this event</h3>
                        
                        <form action="/rate-event/<%= event.id %>" method="POST">
                            <div class="rating-stars" id="starRating">
                                <i class="far fa-star star" data-rating="1"></i>
                                <i class="far fa-star star" data-rating="2"></i>
                                <i class="far fa-star star" data-rating="3"></i>
                                <i class="far fa-star star" data-rating="4"></i>
                                <i class="far fa-star star" data-rating="5"></i>
                            </div>
                            <input type="hidden" id="ratingValue" name="rating" value="0">
                            
                            <div class="form-group">
                                <label for="comment">Add a comment</label>
                                <textarea id="comment" name="comment" placeholder="Share your thoughts about this event..."></textarea>
                            </div>
                            
                            <button type="submit" class="btn">Submit Review</button>
                        </form>
                    </div>
                <% } %>
                
                <div class="comments-section">
                    <h3>Event Reviews</h3>
                    
                    <% if (event.comments && event.comments.length > 0) { %>
                        <% event.comments.forEach(comment => { %>
                            <div class="comment">
                                <div class="comment-header">
                                    <span class="comment-user"><%= comment.username %></span>
                                    <span class="comment-rating">
                                        <% for (let i = 0; i < 5; i++) { %>
                                            <% if (i < comment.rating) { %>
                                                <i class="fas fa-star"></i>
                                            <% } else { %>
                                                <i class="far fa-star"></i>
                                            <% } %>
                                        <% } %>
                                    </span>
                                </div>
                                <div class="comment-text">
                                    <%= comment.text %>
                                </div>
                                <div class="comment-meta" style="font-size: 0.8rem; color: #777;">
                                    Posted on <%= comment.created_at.toLocaleDateString() %>
                                </div>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <div class="no-comments">
                            <p>No reviews yet. Be the first to rate this event!</p>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
        
        <footer>
            <p>&copy; 2025 Campus Connect. All rights reserved.</p>
        </footer>
    </div>

    <script>
        // Star rating functionality
        document.addEventListener('DOMContentLoaded', function() {
            const stars = document.querySelectorAll('.star');
            const ratingInput = document.getElementById('ratingValue');
            
            stars.forEach(star => {
                star.addEventListener('click', function() {
                    const rating = parseInt(this.getAttribute('data-rating'));
                    ratingInput.value = rating;
                    
                    // Update star display
                    stars.forEach((s, index) => {
                        if (index < rating) {
                            s.classList.remove('far');
                            s.classList.add('fas', 'active');
                        } else {
                            s.classList.remove('fas', 'active');
                            s.classList.add('far');
                        }
                    });
                });
            });
        });
    </script>
</body>
</html>