<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        :root {
            --gold: #FFD700;
            --dark-gold: #D4AF37;
            --black: #000000;
            --white: #FFFFFF;
            --light-gray: #F5F5F5;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--black);
            color: var(--white);
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            border-bottom: 2px solid var(--gold);
            margin-bottom: 30px;
        }
        
        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--gold);
        }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .user-info span {
            color: var(--gold);
        }
        
        .logout-btn {
            background-color: var(--gold);
            color: var(--black);
            border: none;
            padding: 8px 15px;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        
        .logout-btn:hover {
            background-color: var(--dark-gold);
        }
        
        .page-title {
            color: var(--gold);
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-align: center;
        }
        
        .tabs {
            display: flex;
            margin-bottom: 30px;
            border-bottom: 1px solid var(--gold);
        }
        
        .tab {
            padding: 10px 20px;
            cursor: pointer;
            color: var(--gold);
            font-weight: bold;
            border-bottom: 3px solid transparent;
            transition: all 0.3s;
        }
        
        .tab.active {
            border-bottom: 3px solid var(--gold);
            color: var(--white);
        }
        
        .tab-content {
            display: none;
        }
        
        .tab-content.active {
            display: block;
        }
        
        .events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }
        
        .event-card {
            background-color: var(--white);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 15px rgba(255, 215, 0, 0.2);
            color: var(--black);
        }
        
        .event-header {
            background-color: var(--gold);
            padding: 15px;
            color: var(--black);
            font-weight: bold;
        }
        
        .event-body {
            padding: 20px;
        }
        
        .event-name {
            font-size: 1.3rem;
            margin-bottom: 10px;
            color: var(--black);
        }
        
        .event-meta {
            color: #666;
            margin-bottom: 5px;
            font-size: 0.9rem;
        }
        
        .event-description {
            margin: 15px 0;
            color: #444;
        }
        
        .btn {
            display: inline-block;
            background-color: var(--gold);
            color: var(--black);
            padding: 10px 20px;
            border-radius: 4px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
            border: none;
            cursor: pointer;
            margin-top: 10px;
        }
        
        .btn:hover {
            background-color: var(--dark-gold);
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: var(--gold);
        }
        
        .form-group input, 
        .form-group select, 
        .form-group textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
            background-color: var(--light-gray);
        }
        
        .form-group textarea {
            min-height: 100px;
            resize: vertical;
        }
        
        .admin-badge {
            background-color: var(--gold);
            color: var(--black);
            padding: 3px 8px;
            border-radius: 3px;
            font-size: 0.8rem;
            margin-left: 10px;
        }
        
        footer {
            text-align: center;
            margin-top: 50px;
            padding: 20px 0;
            border-top: 2px solid var(--gold);
            color: var(--gold);
        }
        
        .no-events {
            grid-column: 1/-1;
            text-align: center;
            color: var(--gold);
            padding: 40px;
            background-color: rgba(255, 215, 0, 0.1);
            border-radius: 8px;
        }
    </style>
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">UCF EVENTS</div>
            <div class="user-info">
                <% if (user) { %>
                    <span><%= user.username %></span>
                    <% if (user.isAdmin) { %>
                        <span class="admin-badge">ADMIN</span>
                    <% } %>
                    <% if (user.isSuperAdmin) { %>
                        <span class="superadmin-badge">SUPER ADMIN</span>
                    <% } %>
                    <a href="/logout" class="logout-btn">Logout</a>
                <% } else { %>
                    <a href="/login" class="login-btn">Login</a>
                <% } %>
            </div>
        </header>
        
        <h1 class="page-title">Campus Events</h1>
        
        <% if (user && (user.isAdmin || user.isSuperAdmin)) { %>
            <div class="host-event-section">
                <h2>Host a New Event</h2>
                <form id="eventForm" action="/create-event" method="POST">
                    <div class="form-group">
                        <label for="eventName">Event Name</label>
                        <input type="text" id="eventName" name="name" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="category">Category</label>
                        <select id="category" name="category" required>
                            <option value="">Select a category</option>
                            <option value="Academic">Academic</option>
                            <option value="Social">Social</option>
                            <option value="Sports">Sports</option>
                            <option value="Cultural">Cultural</option>
                            <option value="Workshop">Workshop</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" required></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label for="date">Date</label>
                        <input type="date" id="date" name="date" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="time">Time</label>
                        <input type="time" id="time" name="time" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="location">Location</label>
                        <input type="text" id="location" name="location" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="contactInfo">Contact Info</label>
                        <input type="tel" id="contactInfo" name="contact_info" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="contactEmail">Contact Email</label>
                        <input type="email" id="contactEmail" name="contact_email" value="<%= user.email %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="event_type">Event Type</label>
                        <select id="event_type" name="event_type" required>
                            <option value="Public">Public (Visible to everyone)</option>
                            <option value="Private">Private (Only for <%= user.university %> students)</option>
                            <option value="RSO">RSO Event (Only for RSO members)</option>
                        </select>
                    </div>
                    
                    <% if (user.university) { %>
                        <input type="hidden" name="university_id" value="<%= user.university %>">
                    <% } %>
                    
                    <div id="rsoField" class="form-group" style="display: none;">
                        <label for="rso_id">Select RSO</label>
                        <select id="rso_id" name="rso_id">

                        </select>
                    </div>
                    
                    <button type="submit" class="btn">Create Event</button>
                </form>
            </div>
        <% } %>
        
        <div class="events-section">
            <h2>Upcoming Events</h2>
            
            <% if (!user) { %>
                <div class="info-banner">
                    <i class="fas fa-info-circle"></i> 
                    <p>You're viewing public events only. <a href="/login">Login</a> to see private and RSO events available to you.</p>
                </div>
            <% } %>
            
            <div class="events-grid">
                <% if (events && events.length > 0) { %>
                    <% for(let i = 0; i < events.length; i++) { %>
                        <div class="event-card">
                            <div class="event-header">
                                <%= events[i].category %>
                                <span class="event-type-badge <%= events[i].event_type.toLowerCase() %>">
                                    <%= events[i].event_type %>
                                    <% if (events[i].event_type === 'Private') { %>
                                        <i class="fas fa-lock" title="Only visible to <%= events[i].university_name || events[i].university_id %> students"></i>
                                    <% } else if (events[i].event_type === 'RSO') { %>
                                        <i class="fas fa-users" title="Only visible to RSO members"></i>
                                    <% } else { %>
                                        <i class="fas fa-globe" title="Public event"></i>
                                    <% } %>
                                </span>
                            </div>
                            <div class="event-body">
                                <div class="event-name"><%= events[i].name %></div>
                                <div class="event-meta">
                                    <i class="far fa-calendar-alt"></i> <%= new Date(events[i].date).toLocaleDateString() %> at <%= events[i].time %>
                                </div>
                                <div class="event-meta">
                                    <i class="fas fa-map-marker-alt"></i> <%= events[i].location %>
                                </div>
                                <div class="event-description">
                                    <%= events[i].description %>
                                </div>
                                <% if (events[i].university_name) { %>
                                    <div class="event-meta">
                                        <i class="fas fa-university"></i> <%= events[i].university_name %>
                                    </div>
                                <% } %>
                                <div class="event-meta">
                                    <i class="fas fa-envelope"></i> <%= events[i].contact_email %>
                                </div>
                                <div class="event-meta">
                                    <i class="fas fa-phone"></i> <%= events[i].contact_info %>
                                </div>
                                <a href="/viewEvent/<%= events[i].id %>" class="btn">View Details</a>
                            </div>
                        </div>
                    <% } %>
                <% } else { %>
                    <div class="no-events">
                        <h3>No upcoming events</h3>
                        <% if (user && (user.isAdmin || user.isSuperAdmin)) { %>
                            <p>Create the first event using the form above!</p>
                        <% } else { %>
                            <p>Check back later for upcoming events</p>
                        <% } %>
                    </div>
                <% } %>
            </div>
        </div>
        
        <footer>
            <p>&copy; 2025 Campus Connect. All rights reserved.</p>
        </footer>
    </div>

    <script>
        // Set minimum date to today for event creation
        document.addEventListener('DOMContentLoaded', function() {
            const today = new Date().toISOString().split('T')[0];
            if (document.getElementById('date')) {
                document.getElementById('date').min = today;
            }

            // Show/hide RSO field based on event type selection
            const eventTypeSelect = document.getElementById('event_type');
            const rsoField = document.getElementById('rsoField');
            
            if (eventTypeSelect && rsoField) {
                eventTypeSelect.addEventListener('change', function() {
                    if (this.value === 'RSO') {
                        rsoField.style.display = 'block';
                        // Load RSOs for this user
                        fetch('/get-rsos')
                            .then(response => response.json())
                            .then(rsos => {
                                const select = document.getElementById('rso_id');
                                select.innerHTML = '';
                                rsos.forEach(rso => {
                                    const option = document.createElement('option');
                                    option.value = rso.id;
                                    option.textContent = rso.name;
                                    select.appendChild(option);
                                });
                            });
                    } else {
                        rsoField.style.display = 'none';
                    }
                });
            }
        });
    </script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</body>
</html>
